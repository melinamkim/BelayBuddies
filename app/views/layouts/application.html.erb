<!DOCTYPE html>
<html>
  <head>
    <%= favicon_link_tag 'favicon.ico' %>
    <title>BelayBuddies: Find a climbing partner that fits around your schedule</title>
    <meta name="description" content="Belay Buddies is a way for new and experienced climbers to find climbing partners based around their busy schedules.">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link href="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.js"></script>
  </head>

  <body>
    <%= render "shared/navbar" %>
    <div style="height: 65px"> </div>
    <%= render "shared/flashes" %>
    <div style="min-height: calc(100vh - 166.23px);">
      <%= yield %>
    </div>
    <%= render "shared/footer" %>
  </body>
</html>
