  <div class="container">
    <h1 class="heading text-warning fw-bolder">🧗 <strong> Climbing Buddies Matches </strong> 🧗</h1>
    <div class="cards">
        <% @users.each do |user| %>

            <%= link_to user_path(user) do %>
              <div class="card-trip">
                <%# <%= cl_image_tag user.photo.key, height: 300, width: 500, crop: :fill %>
                <img src="https://images.unsplash.com/photo-1610768798760-e01a4b43814e?ixlib=rb-1.2.1&ixid=MnwxM[…]90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=3540&q=80" alt="">
                <div class="card-trip-infos">
                  <div>
                    <span><%= user.full_name %></span>
                    <%= user.gender == "male" ? '<i class="fa-solid fa-mars"></i>'.html_safe : '<i class="fa-solid fa-venus"></i>'.html_safe %>
                    <br>
                    <%= user.level %>
                    <br>
                    📍 <span class="locationcolor"><%= user.location %></span>
                    <br>
                    <span><%= user.types_of_climbing %></span>
                  </div>
                </div>
              </div>
            <% end %>
        <% end %>
    </div>
<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
