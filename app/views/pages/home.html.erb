<div class="banner" style="background-image: url(<%= asset_path('stroghold_2.jpg') %>)">
  <div class="container text-center">
    <br>
      <p>Find a climbing <span data-controller="typed-js"></span></p>

    <%# <div class="container h-100">
      <div class="d-flex justify-content-center h-100">
        <div class="searchbar">
          <input class="search_input" type="text" name="" placeholder="Look for Buddies...">
          <a href="#" class="search_icon"><i class="fas fa-search"></i></a>
        </div>
      </div>
    </div> %>


    <form action='/users' class="align-items-center" id="searchbar">
      <div class="col-12">
        <label class="visually-hidden" for="inlineFormInputGroupUsername">Username</label>
        <div class="input-group">
          <input type="text" class="form-control fs-5 round" id="inlineFormInputGroupUsername" placeholder="Find Belay Buddies in London" name='location'>
       </div>
       <br>
        <input type="submit" value="Let's Go!">
      </div>
    </form>
  </div>
</div>

  <div class="box-container text-center">
    <h1 class="heading fw-bolder mt-4 mb-4">🧗‍♀️ Recently Joined Buddies 🧗🏽‍♂️</h1>
    <div class="cards text-center">
        <% @users.each do |user| %>
          <div class="position-relative">
            <%= link_to user_path(user) do %>
              <div class="card-user">
                <%= cl_image_tag user.photo.key %>
                <h2 class="user-name mt-3 fw-bolder"><%= user.full_name %></h2>
                <div class="card-user-infos">
                  <p>📍<%= user.location %></p>
                    <% user.types_of_climbing.each do |type_of_climbing| %>
                    <p class="mb-0"><%= type_of_climbing %></p>
                    <% end %>
                </div>
              </div>
            <% end %>
            <div class="d-flex justify-content-center align-items-center card-btn p-0 fw-bolder">
              <% chatroom = Chatroom.where(receiver: user, requester: current_user).first || Chatroom.where(receiver: current_user, requester: user).first %>
              <% if chatroom %>
                <%= link_to 'Chat', chatroom_path(chatroom), class: "btn" %>
              <% else %>
                <%= simple_form_for [user, Chatroom.new] do |f| %>
                  <%= f.input :receiver_id, as: :hidden, input_html: { value: user.id } %>
                  <%= f.submit 'Chat', class: 'btn btn-lg' %>
                <% end %>
              <% end %>
            </div>
          </div>
        <% end %>
    </div>
  </div>
<div class="wrapper text-center">
  <h1>Nearest Climbing Gyms</h1>
</div>

<div class="wrapper text-center">
  <h1>Check out what's happening in the climbing world!</h1>
</div>
